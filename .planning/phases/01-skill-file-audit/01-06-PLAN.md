---
phase: 01-skill-file-audit
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/generar-preguntas.md
  - .claude/commands/generar-preguntas/tipos-preguntas.md
  - .claude/commands/generar-preguntas/verificacion.md
autonomous: true
requirements: [DATA-01, SKILL-01, SKILL-02, SKILL-03]
must_haves:
  truths:
    - "Generation skill files no longer reference todotest_2700.json, dgt_oficial_exam.json, hardest_*.json, or content-structure.json"
    - "verificacion.md no longer contains the 58-line subtema mapping table — only the 7 common-mistake lines remain"
    - "Signal handling rules exist in exactly one authoritative location (verificacion.md), not duplicated across 3 files"
    - "generar-preguntas.md subagent section instructs conditional file reading per type, not all 10 files"
    - "No content is duplicated between generar-preguntas.md and its sub-files (tipos-preguntas.md, verificacion.md, per-type files)"
  artifacts:
    - path: ".claude/commands/generar-preguntas.md"
      provides: "Streamlined main skill file — workflow, JSON format, subagent dispatch"
      contains: "solo el archivo del tipo"
    - path: ".claude/commands/generar-preguntas/verificacion.md"
      provides: "Consolidated verification rules with signal handling as authoritative source"
      contains: "Campo codigo_señal"
    - path: ".claude/commands/generar-preguntas/tipos-preguntas.md"
      provides: "Type distribution and difficulty levels without reference file pointers"
  key_links:
    - from: ".claude/commands/generar-preguntas.md"
      to: "verificacion.md"
      via: "Read directive for signal rules"
      pattern: "verificacion.md"
    - from: ".claude/commands/generar-preguntas.md"
      to: "per-type files"
      via: "Conditional read per tipo_pregunta"
      pattern: "solo el archivo del tipo"
---

<objective>
Remove token-heavy references, deduplicate cross-file content, and restructure conditional file reading in the 3 generation skill files (generar-preguntas.md, tipos-preguntas.md, verificacion.md).

Purpose: Reduce the ~47k+ token load per subagent by eliminating references to files totaling ~519k tokens (todotest_2700.json, hardest_*.json, dgt_oficial_exam.json, content-structure.json), removing the redundant 58-line subtema mapping table, consolidating signal handling to one location, and making subagents read only the files they need.

Output: Three cleaned skill files with no dead references, no cross-file duplication, and conditional reading instructions.
</objective>

<execution_context>
@C:/Users/fangz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/fangz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-skill-file-audit/01-CONTEXT.md
@.claude/commands/generar-preguntas.md
@.claude/commands/generar-preguntas/tipos-preguntas.md
@.claude/commands/generar-preguntas/verificacion.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean generar-preguntas.md — remove dead references, deduplicate, restructure subagents</name>
  <files>.claude/commands/generar-preguntas.md</files>
  <action>
Edit `.claude/commands/generar-preguntas.md` with these specific changes:

**1. Remove "Fuentes de datos" section (lines 60-62):**
Delete the 3-line block listing "2.700 preguntas extraidas de 90 tests de Todotest", "30 preguntas del examen oficial DGT", and "Temario oficial". The temario reference is already in Step 1 of the Workflow. The other two are dead references.

**2. Remove content-structure.json from Workflow Step 1 (line 75):**
Delete `2. Lee la estructura en content/content-structure.json` — tema files already have `[subtema_XX]` markers. Renumber remaining steps.

**3. Restructure subagent section (Step 5, lines 98-114) for conditional reading:**
Replace the current flat list of 10 files with a tiered structure:

```
### 5. Subagentes (si >30 preguntas)
- **1-30 preguntas**: Todo en el hilo principal, sin subagentes
- **31+ preguntas**: Dividir en subagentes (Task tool, subagent_type: "general-purpose", model: "sonnet")

**Cada subagente DEBE leer estos archivos** (NO parafrasear ni resumir):

Siempre:
1. `.claude/commands/generar-preguntas/tipos-preguntas.md` — tipos, distribucion, niveles
2. `.claude/commands/generar-preguntas/datos-numericos.md` — valores numericos exactos
3. `.claude/commands/generar-preguntas/verificacion.md` — reglas de verificacion, senales, autosuficiencia
4. `.claude/commands/generar-preguntas/explicaciones.md` — formato de explicaciones
5. `.claude/commands/generar-preguntas/patrones-y-trampas.md` — patrones de inicio, palabras trampa

Solo el archivo del tipo asignado:
6. `.claude/commands/generar-preguntas/[tipo].md` — (dato.md, directo.md, completar.md o situacional.md)

Solo los temas asignados:
7. `content/temario/tema_XX.md` — solo los temas del bloque asignado

Solo si genera preguntas sobre senales:
8. `content/imagenes/senales/catalogo.json` — codigos de senales (solo cuando la pregunta trata de una senal especifica)
```

**4. Remove signal handling duplication from generar-preguntas.md:**
The "Pistas" section (lines 123-125) is duplicated in verificacion.md (Campo pista section). Remove the 3-line "Pistas" mini-section. The full rules are in verificacion.md.

The "Campo `codigo_señal`" section (lines 151-155), "Reglas de senales en el enunciado" section (lines 157-165), and "Catalogo de senales disponibles" section (lines 172-173) are ALL duplicated in verificacion.md (Senales de trafico e imagenes section). Remove all three from generar-preguntas.md. The authoritative signal rules stay in verificacion.md. Add a one-line pointer: `Para reglas de señales y campo codigo_señal, ver verificacion.md.`

**5. Clean "Archivos de referencia" section (lines 175-184):**
Remove these dead references:
- `content/content-structure.json`
- `content/todotest_2700.json`
- `content/dgt_oficial_exam.json`
- All 4 `content/hardest_*.json` lines

Keep only:
- Temario: `temario_permiso_b_v3.md`
- Catalogo de senales: `content/imagenes/senales/catalogo.json` (235 senales con SVGs)
- Salida: `content/preguntas/`

**6. Keep the JSON format section exactly as-is** (lines 127-149) — this is the canonical schema definition.

**7. Keep "Regla Fundamental: Escenario Primero" exactly as-is** — this is core content that is NOT duplicated elsewhere.
  </action>
  <verify>
    <automated>grep -c "todotest_2700\|hardest_\|dgt_oficial_exam\|content-structure" .claude/commands/generar-preguntas.md | grep "^0$"</automated>
    <manual>Verify the file reads cleanly, has no broken references, and the subagent section shows conditional reading tiers</manual>
  </verify>
  <done>generar-preguntas.md contains zero references to todotest_2700.json, hardest_*.json, dgt_oficial_exam.json, or content-structure.json. Subagent section shows 3 tiers (always/per-type/conditional). Signal rules point to verificacion.md. Pistas mini-section removed.</done>
</task>

<task type="auto">
  <name>Task 2: Clean tipos-preguntas.md and verificacion.md — remove references, consolidate signal rules, trim subtema table</name>
  <files>.claude/commands/generar-preguntas/tipos-preguntas.md, .claude/commands/generar-preguntas/verificacion.md</files>
  <action>
**tipos-preguntas.md edits:**

1. **Remove calibration note at top (line 1-3):** Delete the blockquote `> **Calibracion de dificultad**: Todas las preguntas deben imitar... Consultar content/hardest_dato.json y equivalentes para calibracion de dificultad.` — the per-type files now provide difficulty calibration.

2. **Remove hardest_*.json references from each type section:**
   - Line 70: Remove `(nivel objetivo — ver content/hardest_directa.json para 57 mas)` — change to just `(nivel objetivo):`
   - Line 87: Remove `(ver content/hardest_situacional.json para 84 mas)` — change to just parenthetical without file reference
   - Line 101: Remove `(ver content/hardest_completar.json para 133 mas)` — same
   - Line 113: Remove `(ver content/hardest_dato.json para 457 mas)` — same

3. **The signal handling note in "Tecnicas transversales" (line 17)** stays as a brief pointer. It already says "NO incluir campos requiere_imagen ni tipo_imagen" and refers to codigo_señal. This is fine as a one-liner reminder. Do NOT remove it.

**verificacion.md edits:**

1. **Delete the 58-line subtema mapping table (lines 5-66):**
Replace the full table with ONLY the 7 "Errores frecuentes a evitar" lines (lines 68-76). Change the section header from "Asignacion de subtema_id (OBLIGATORIO — leer antes de escribir cualquier pregunta)" to "Asignacion de subtema_id".

The new section should read:
```
## Asignacion de subtema_id

El subtema_id se asigna segun el CONTENIDO de la pregunta, no segun el tema pedido. Consultar los marcadores [subtema_XX] en los archivos tema_XX.md para la lista completa.

**Errores frecuentes a evitar:**
- Pregunta sobre parada/estacionamiento -> subtema_23 (no subtema_21 Adelantamientos)
- Pregunta sobre fatiga/sueno -> subtema_46 (no subtema_43 Alcohol)
- Pregunta sobre el movil al volante -> subtema_47 Distracciones (no subtema_15)
- Pregunta sobre circular en autopista/autovia (prohibiciones, normas especificas) -> subtema_32 (no subtema_16 Velocidad)
- Pregunta sobre adelantar ciclistas -> subtema_21 (no subtema_35 Condiciones adversas)
- Pregunta sobre velocidad inadecuada vs exceso -> subtema_16 Velocidad (no subtema_35 ni subtema_48)
- Pregunta sobre marcha atras (distancia, prohibiciones) -> subtema_18 (no subtema_35)
```

2. **Remove "Archivos de referencia de dificultad" block (lines 319-324):**
Delete the 4 hardest_*.json references and the "Antes de generar, consultar estos archivos" header. The per-type files already contain this calibration.

3. **Remove "Mezcla de dificultad por test" block (lines 326-331):**
This is duplicated in tipos-preguntas.md difficulty levels section — types-preguntas.md is the authoritative source for distribution. Delete this block from verificacion.md.

4. **Consolidate signal rules in verificacion.md:**
verificacion.md already has the "Senales de trafico e imagenes" section (lines 184-195). This section stays and becomes the SOLE authoritative location. The HARD REJECT row about signal codes in enunciado (line 91) and the checklist item about signal codes (line 312) also stay — they reference this section. No changes needed to these since generar-preguntas.md will point here.
  </action>
  <verify>
    <automated>bash -c "grep -c 'hardest_' .claude/commands/generar-preguntas/tipos-preguntas.md .claude/commands/generar-preguntas/verificacion.md | grep -v ':0$' | wc -l" | grep "^0$"</automated>
    <manual>Verify subtema table in verificacion.md has ~12 lines (header + 7 error lines), not 58+ lines. Verify no hardest_*.json references remain in either file.</manual>
  </verify>
  <done>tipos-preguntas.md has zero hardest_*.json references and no calibration blockquote. verificacion.md has subtema section reduced from 66 lines to ~12 lines, zero hardest_*.json references, and "Mezcla de dificultad por test" block removed. Signal handling rules remain consolidated in verificacion.md.</done>
</task>

</tasks>

<verification>
Run these checks after both tasks:

1. `grep -rn "todotest_2700\|hardest_\|dgt_oficial_exam\|content-structure" .claude/commands/generar-preguntas.md .claude/commands/generar-preguntas/tipos-preguntas.md .claude/commands/generar-preguntas/verificacion.md` — should return zero matches
2. `wc -l .claude/commands/generar-preguntas/verificacion.md` — should be notably shorter than current ~331 lines (target: under 280)
3. `wc -l .claude/commands/generar-preguntas.md` — should be notably shorter than current ~184 lines (target: under 130)
4. Grep for "subtema_01" in verificacion.md should return 0 matches (table removed)
5. Grep for "codigo_señal" in generar-preguntas.md should return only the JSON format example (1 match in the schema, not in signal rules sections)
</verification>

<success_criteria>
- All 3 generation skill files have zero references to dead files (todotest_2700.json, hardest_*.json, dgt_oficial_exam.json, content-structure.json)
- verificacion.md subtema section is 7 error lines + brief header, not 58-line full table
- Signal handling rules exist in one place (verificacion.md), with pointers from generar-preguntas.md and tipos-preguntas.md
- Subagent section in generar-preguntas.md uses conditional reading tiers
- No meaningful content is lost — only duplicated or dead content is removed
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-file-audit/01-06-SUMMARY.md`
</output>
