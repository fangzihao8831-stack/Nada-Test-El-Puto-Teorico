---
phase: 01-skill-file-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .claude/commands/validar-preguntas/datos-referencia.md
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "Every value in datos-referencia.md that also appears in datos-numericos.md is identical to the value in datos-numericos.md"
    - "datos-referencia.md includes a points section (8 / 12 / 15 points, alcohol penalty points) for validator lookup"
    - "datos-referencia.md contains no value that contradicts the corrected datos-numericos.md"
  artifacts:
    - path: ".claude/commands/validar-preguntas/datos-referencia.md"
      provides: "Validator quick-lookup reference for Haiku CHECK 4"
      contains: "puntos"
  key_links:
    - from: ".claude/commands/validar-preguntas/datos-referencia.md"
      to: ".claude/commands/generar-preguntas/datos-numericos.md"
      via: "manual sync — shared values must be identical"
      pattern: "no contradictions between shared values"
---

<objective>
Sync datos-referencia.md to be consistent with the corrected datos-numericos.md from Plan 01-01. datos-referencia.md is read by the Haiku validator during CHECK 4 as a quick-lookup table. It can be a subset of datos-numericos.md but must not contradict it.

Purpose: datos-referencia.md was not updated when datos-numericos.md last received corrections, creating a known drift. The Haiku validator uses datos-referencia.md as its evidence source — if it contains a wrong value (e.g., old trailer speed), it will validate wrong answers as correct.

Output: A corrected datos-referencia.md that is a clean, validator-focused subset of datos-numericos.md with no contradictions, including the missing puntos section.
</objective>

<execution_context>
@C:/Users/fangz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/fangz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-file-audit/01-CONTEXT.md
@.planning/phases/01-skill-file-audit/01-RESEARCH.md
@.planning/phases/01-skill-file-audit/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync datos-referencia.md to match corrected datos-numericos.md</name>
  <files>.claude/commands/validar-preguntas/datos-referencia.md</files>
  <action>
Read `.claude/commands/validar-preguntas/datos-referencia.md` in full to understand its current structure and all values it contains.

Read `.claude/commands/generar-preguntas/datos-numericos.md` (corrected in Plan 01-01) in full.

Also read `.claude/commands/validar-preguntas/check-4-datos.md` to understand what categories the validator actively looks up during CHECK 4. This determines which sections are genuinely needed in datos-referencia.md.

**Per the locked decision:** datos-referencia.md may be a simplified subset of datos-numericos.md. It must include only the values the validator uses actively. It must NOT contradict datos-numericos.md in any shared value.

**Sync process — work category by category:**

1. **Speed limits**: For each vehicle category in datos-referencia.md, verify the value matches datos-numericos.md exactly. If datos-referencia.md is missing a vehicle category (e.g., "Autobus con pasajeros de pie" added in Plan 01-01), add it. If datos-referencia.md has a value that now differs from datos-numericos.md, update datos-referencia.md to match.

2. **Remolque speeds**: Same sync process. All values must match datos-numericos.md.

3. **Emergency signaling**: Update to reflect the V-16 / triangle correction from Plan 01-01. Remove any reference to "150 m" if present.

4. **Alcohol limits**: Verify all limits (general, novel, professional) match datos-numericos.md exactly.

5. **ITV periods**: Verify turismo, motocicleta, ciclomotor rows match datos-numericos.md.

6. **Parking distances**: Verify all distance values match datos-numericos.md.

7. **SRI**: Verify 135 cm threshold matches.

8. **Puntos del permiso (ADD — this section is MISSING per Pitfall 7 in RESEARCH.md):**
   - datos-referencia.md currently omits the points section entirely.
   - Add a minimal "Puntos del permiso" section with:
     - Conductor novel: 8 puntos
     - Tras 2 anos sin sanciones (novel): 12 puntos
     - Maximo permitido: 15 puntos
     - Penalizacion por alcohol (tasa entre 0,25 y 0,50 mg/l aire): 4 puntos
     - Penalizacion por alcohol (tasa superior a 0,50 mg/l aire): 6 puntos
   - Source values from datos-numericos.md (already corrected with citations in Plan 01-01).
   - This section is needed because the Haiku validator encounters points questions and has no reference source currently.

**Claude's discretion on what to include/omit:**
- Include a section if the Haiku validator is likely to encounter questions from that domain.
- Omit or compress sections that are extremely unlikely to appear in CHECK 4 verifications.
- Structure and formatting choices are at Claude's discretion — the goal is a compact, validator-friendly reference, not a copy of datos-numericos.md.

Do NOT copy citations (e.g., `(tema_05.md)`) from datos-numericos.md into datos-referencia.md unless they are helpful for the validator. datos-referencia.md is a lookup table, not a documented reference.
  </action>
  <verify>
    <automated>grep -i "puntos\|permiso" ".claude/commands/validar-preguntas/datos-referencia.md"</automated>
    <manual>
      1. Open datos-referencia.md. Confirm it has a "Puntos del permiso" section with the 8/12/15 values.
      2. Find the trailer/remolque speed section. Confirm it matches datos-numericos.md (e.g., remolque ligero on autopista should be 90 km/h, not 80 km/h — this was the known root-cause discrepancy).
      3. Check the emergency/triangle section — confirm no "150 m" value is present.
      4. Pick 3 speed values at random and verify they are identical between datos-referencia.md and datos-numericos.md.
    </manual>
    <sampling_rate>Run after this task completes</sampling_rate>
  </verify>
  <done>
    - datos-referencia.md contains a "Puntos del permiso" section with the 8 / 12 / 15 / penalty point values
    - Every shared value between datos-referencia.md and datos-numericos.md is identical
    - No value in datos-referencia.md contradicts datos-numericos.md
    - The "150 m autopista triangle" value is not present in datos-referencia.md
  </done>
</task>

</tasks>

<verification>
After plan completion:
```bash
grep -i "puntos\|permiso" ".claude/commands/validar-preguntas/datos-referencia.md"
grep -i "150 m\|150m" ".claude/commands/validar-preguntas/datos-referencia.md"
```

Expected:
- "puntos" grep returns matches (section exists)
- "150 m" grep returns 0 results
</verification>

<success_criteria>
datos-referencia.md is a clean, consistent subset of datos-numericos.md: no contradictions, no missing critical categories (including puntos), and no unsourced values that differ from the corrected datos-numericos.md.
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-file-audit/01-02-SUMMARY.md`
</output>
