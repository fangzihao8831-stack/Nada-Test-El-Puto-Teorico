---
phase: 01-skill-file-audit
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/validar-preguntas.md
  - .claude/commands/validar-preguntas/check-1-schema.md
  - .claude/commands/validar-preguntas/check-3-duplicados.md
  - .claude/commands/validar-preguntas/check-4-datos.md
  - .claude/commands/validar-preguntas/datos-referencia.md
  - nadatest/scripts/validate-questions.mjs
  - nadatest/scripts/gen-batch02-only.mjs
  - nadatest/scripts/generate-question-bank.mjs
  - nadatest/src/types/database.ts
  - nadatest/src/lib/data/tests.ts
  - nadatest/src/lib/actions/admin.ts
  - nadatest/src/app/(exam)/test/[id]/page.tsx
  - content-pipeline.md
  - technical.md
autonomous: true
requirements: [DATA-02, DATA-03, SKILL-04]
must_haves:
  truths:
    - "Validation skill files no longer reference todotest_2700.json"
    - "check-1-schema.md does not list requiere_imagen or tipo_imagen as required or optional fields"
    - "validate-questions.mjs does not check for or reference requiere_imagen or tipo_imagen"
    - "TypeScript type database.ts does not include requiere_imagen or tipo_imagen properties"
    - "content-pipeline.md JSON example and field table do not include requiere_imagen or tipo_imagen"
  artifacts:
    - path: ".claude/commands/validar-preguntas/check-1-schema.md"
      provides: "Schema validation rules without image metadata fields"
    - path: "nadatest/scripts/validate-questions.mjs"
      provides: "Validation script without image metadata checks"
    - path: "nadatest/src/types/database.ts"
      provides: "Database types without image metadata properties"
    - path: "content-pipeline.md"
      provides: "Content pipeline docs without image metadata fields"
  key_links:
    - from: "check-1-schema.md"
      to: "validate-questions.mjs"
      via: "Both enforce same required fields list"
      pattern: "REQUIRED_FIELDS"
---

<objective>
Remove dead references from validation skill files and clean up deprecated image metadata fields (requiere_imagen, tipo_imagen) across the validation stack, scripts, TypeScript types, and documentation.

Purpose: Validation files still reference todotest_2700.json (~400k tokens loaded unnecessarily by the LLM validator). Image metadata fields were deprecated but remain in the schema validator, the mjs script, TypeScript types, and documentation — causing confusion about what fields new questions should include.

Output: Clean validation files with no dead references, and requiere_imagen/tipo_imagen removed from all layers except existing batch_03 data (which is left untouched per user decision).
</objective>

<execution_context>
@C:/Users/fangz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/fangz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-skill-file-audit/01-CONTEXT.md
@.claude/commands/validar-preguntas.md
@.claude/commands/validar-preguntas/check-1-schema.md
@.claude/commands/validar-preguntas/check-3-duplicados.md
@.claude/commands/validar-preguntas/check-4-datos.md
@.claude/commands/validar-preguntas/datos-referencia.md
@nadatest/scripts/validate-questions.mjs
@nadatest/src/types/database.ts
@content-pipeline.md
@technical.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove todotest_2700.json references from validation skill files</name>
  <files>.claude/commands/validar-preguntas.md, .claude/commands/validar-preguntas/check-3-duplicados.md, .claude/commands/validar-preguntas/check-4-datos.md, .claude/commands/validar-preguntas/datos-referencia.md</files>
  <action>
**validar-preguntas.md:**
1. Remove the line referencing `content/todotest_2700.json` from the references list (line 24: `2. content/todotest_2700.json - 2.700 preguntas extraidas de examen real`). Renumber remaining items.
2. Remove the line `- Todotest: content/todotest_2700.json` from the reference section at the bottom (line 92).

**check-3-duplicados.md:**
1. Remove the line `- content/todotest_2700.json — es material fuente, las preguntas PUEDEN reutilizarse` (line 12). This reference instructed the validator to load 400k tokens of data that is no longer needed — the duplicate check runs against the batch itself and existing content/preguntas/ files.

**check-4-datos.md:**
1. Remove the line `2. content/todotest_2700.json - buscar preguntas sobre el mismo tema` (line 7). Renumber remaining steps. The temario files are the source of truth for fact-checking, not the Todotest bank.

**datos-referencia.md:**
1. Remove the line `- Todotest: content/todotest_2700.json` from the references section at the bottom (line 101).

**Important:** Do NOT remove any references to temario files (tema_XX.md) or catalogo.json — only todotest_2700.json.
  </action>
  <verify>
    <automated>grep -rn "todotest_2700" .claude/commands/validar-preguntas.md .claude/commands/validar-preguntas/check-3-duplicados.md .claude/commands/validar-preguntas/check-4-datos.md .claude/commands/validar-preguntas/datos-referencia.md | wc -l | grep "^0$"</automated>
    <manual>Verify each file still reads coherently after the line removal — no broken numbering, no dangling references</manual>
  </verify>
  <done>Zero references to todotest_2700.json remain in any validation skill file. All 4 files modified, numbering corrected where applicable.</done>
</task>

<task type="auto">
  <name>Task 2: Remove requiere_imagen and tipo_imagen from schema validator, script, types, and docs</name>
  <files>.claude/commands/validar-preguntas/check-1-schema.md, nadatest/scripts/validate-questions.mjs, nadatest/src/types/database.ts, nadatest/src/lib/data/tests.ts, nadatest/src/lib/actions/admin.ts, nadatest/scripts/gen-batch02-only.mjs, nadatest/scripts/generate-question-bank.mjs, nadatest/src/app/(exam)/test/[id]/page.tsx, content-pipeline.md, technical.md</files>
  <action>
**check-1-schema.md:**
The current "Campos obligatorios" table does NOT list requiere_imagen or tipo_imagen (already removed in a prior edit). Verify this is the case. If either field appears, remove it. No other changes needed.

**validate-questions.mjs:**
The current REQUIRED_FIELDS array (lines 24-35) does NOT include requiere_imagen or tipo_imagen. Verify this. The script does not reference these fields in checkSchema() or checkFormat(). No changes needed in the script itself IF no references exist.

However, check the entire file for any remaining references to these fields (they might appear in comments or autofix logic). If found, remove them.

**nadatest/src/types/database.ts:**
Remove the two properties from the Pregunta type (lines 42-43):
```
  requiere_imagen: boolean;
  tipo_imagen: string;
```
These are deprecated fields that should not be in the type definition for new questions. The database migration still has them (which is fine — columns exist for old data), but the TypeScript type should not require them.

**Also check and clean these files that reference requiere_imagen:**
- `nadatest/src/lib/data/tests.ts` — line 15 has `requiere_imagen: boolean` in a type, line 32 has `hasImage: pregunta.requiere_imagen`. Remove the requiere_imagen property from the type. For the hasImage mapping, change to `hasImage: false` (all questions will get images via the separate image skill; this field is no longer meaningful).
- `nadatest/scripts/gen-batch02-only.mjs` — line 37 has `hasImage: q.requiere_imagen || false`. Change to `hasImage: false`.
- `nadatest/scripts/generate-question-bank.mjs` — line 102 has `hasImage: q.requiere_imagen !== false`. Change to `hasImage: false`.
- `nadatest/src/lib/actions/admin.ts` — lines 28-29, 57-58, 108-109 have requiere_imagen/tipo_imagen. Remove these properties from all three type/interface locations in this file. If any function logic uses these values, remove that logic too.
- `nadatest/src/app/(exam)/test/[id]/page.tsx` — line 35 selects `requiere_imagen` from Supabase, line 62 maps it to `hasImage`. Remove `requiere_imagen` from the select string. Set `hasImage: false` or remove the hasImage mapping if it's not used downstream.

**content-pipeline.md:**
1. Remove `requiere_imagen` and `tipo_imagen` from the JSON example (lines 83-84).
2. Remove the two rows from the field table (lines 96-97): `| requiere_imagen | bool | ... |` and `| tipo_imagen | string | ... |`.

**technical.md:**
1. Remove the two lines from the SQL schema (lines 272-273): `requiere_imagen BOOLEAN DEFAULT FALSE,` and `tipo_imagen TEXT DEFAULT 'ninguna',`.

**IMPORTANT:** Do NOT modify any JSON data files in content/preguntas/ — existing batch_03 data keeps its fields per user decision.
  </action>
  <verify>
    <automated>bash -c "grep -rn 'requiere_imagen\|tipo_imagen' .claude/commands/validar-preguntas/check-1-schema.md nadatest/scripts/validate-questions.mjs nadatest/src/types/database.ts content-pipeline.md technical.md nadatest/src/lib/data/tests.ts nadatest/src/lib/actions/admin.ts nadatest/scripts/generate-question-bank.mjs | wc -l" | grep "^0$"</automated>
    <manual>Verify the Next.js app still builds after type changes: cd nadatest && npx next build (or at minimum check that TypeScript compiles without errors on the modified files)</manual>
  </verify>
  <done>requiere_imagen and tipo_imagen removed from: check-1-schema.md (confirmed absent), validate-questions.mjs (confirmed absent), database.ts, data/tests.ts, actions/admin.ts, generate-question-bank.mjs, gen-batch02-only.mjs, test/[id]/page.tsx, content-pipeline.md, and technical.md. Existing batch_03 JSON data untouched.</done>
</task>

</tasks>

<verification>
Run these checks after both tasks:

1. `grep -rn "todotest_2700" .claude/commands/validar-preguntas/ .claude/commands/validar-preguntas.md` — should return 0 matches
2. `grep -rn "requiere_imagen\|tipo_imagen" .claude/commands/ nadatest/src/types/ nadatest/scripts/ nadatest/src/lib/ nadatest/src/app/ content-pipeline.md technical.md` — should return 0 matches (excluding content/preguntas/ batch data)
3. `node nadatest/scripts/validate-questions.mjs content/preguntas/batch_03` — should still run and produce valid output (batch_03 has the old fields but the script should ignore them gracefully)
4. TypeScript compilation check: `cd nadatest && npx tsc --noEmit 2>&1 | head -20` — should have no new errors related to requiere_imagen or tipo_imagen
</verification>

<success_criteria>
- All validation skill files have zero references to todotest_2700.json
- requiere_imagen and tipo_imagen are removed from all TypeScript types, scripts, and documentation
- Existing batch_03 JSON data is untouched
- validate-questions.mjs still runs correctly on existing batch data
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-file-audit/01-07-SUMMARY.md`
</output>
